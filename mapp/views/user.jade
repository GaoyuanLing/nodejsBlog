//- user.jade
doctype html
html
  include ./includes/head.jade
  each post in posts
    p
      h2 
        a(href='#{post.name}/#{post.title}/#{post.time.day}')= post.title
    p(class= 'info') 作者：
      a(href='#{post.name}')= post.name
      |     | 日期：
      a(href='#')= post.time.miniute
    p!= post.post
  include ./page.jade
  include ./includes/foot.jade
